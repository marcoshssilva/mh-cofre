import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.marcoshssilva.mhcofre',
  appName: 'MHCofre',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
